import numpy as np
import sympy as sy
import pprint
import matplotlib.pyplot as plt
from lab3 import funcs

# a, b = -2, 0
# EPS = 0.5
#
# x = sy.Symbol('x')
# f = x**4 - x**3 - x**2-x -2
#
# x_points = np.arange(a, b, 0.1)
# y_points = list(map(lambda i: f.subs(x, i), x_points))
#
# plt.plot(x_points, y_points)
#
# x_0 = funcs.secant(f, a, b, EPS)
#
# print(f"f({x_0}) = {f.subs(x, x_0)}")
#
# plt.show()

